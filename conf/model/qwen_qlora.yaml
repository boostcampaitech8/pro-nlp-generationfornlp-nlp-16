name: "unsloth/Qwen2.5-32B-Instruct-bnb-4bit"
dtype: "float16" # V100 supports fp16 well (bf16 is Ampere+)

response_template: "<|im_start|>assistant\n"

# QLoRA Quantization Config (BitsAndBytes)
quantization:
  load_in_4bit: true
  bnb_4bit_use_double_quant: true
  bnb_4bit_quant_type: "nf4"
  bnb_4bit_compute_dtype: "float16"

# PEFT Config
peft:
  r: 8
  lora_alpha: 16
  lora_dropout: 0.05
  bias: "none"
  task_type: "CAUSAL_LM"
  # Qwen Target Modules
  target_modules: 
    - "q_proj"
    - "k_proj"
    - "v_proj"
    - "o_proj"
    - "gate_proj"
    - "up_proj"
    - "down_proj"
